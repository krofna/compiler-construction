int x;

void puts(char* c) { return ; }
int sqrt(int x) { return 0; }
void putchar(int x) { return ; }
int getchar(void) { return 0; }

void f(void) {
    int root2;
    x = 2;
    x = 2 + 3;
    2 + 3;
    puts("hi");
    root2 = sqrt(2);

    if(x) {
    x:
        f();
        puts("Hello");
    }

    if(x)
    y:
        f();

    if(x);

    if(x) { f(); } else { f(); }
    if(x) z:f(); else w:f();

    if(x < 0) { f(); } else if(x > 0) { f(); } else { 0; }
    if(x + 0) xx:f(); else if(x - 0) yy:xyz:f(); else zz:f();
    if(x); else if(x); else ;

    int c, EOF;
    
    while((c = getchar()) != EOF) {
        switch(c) {
            int d;
        case 'e':{ x; x; }
        case 'E':
            break;
        default:
            putchar(c);
            break;
        }
    }

    while(x--);
    while(x+0)
        x--;
    
    return ;
}

int g(void) {

    if(x) if(x) ; else if(x) ; else ; // Is this indented ok?

    return 0;
}
